"""CLI command implementations — Phase C Split.

Each module implements one or more related subcommands.

TODO(econtools): cli-cmd — implement data.py (load, describe, summary, findcols)
TODO(econtools): cli-cmd — implement analysis.py (regress, iv, fd, probit)
TODO(econtools): cli-cmd — implement output.py (texpdf, project)
"""
