# refresh

Restore full project context at the start of a new session.

Read both files now before doing anything else:

1. **Progress log** — phases done, deliverables, next steps, gotchas:
   `C:\Econometrics\.claude\progress.md`

2. **Active memory** — in-flight decisions, open questions, architecture notes:
   `C:\Econometrics\.claude\active_memory.md`

After reading both, summarise:
- What phase we are on and what is complete
- What the immediate next task is
- Any open decisions that need resolving before work begins
